/**
 * Hydration reminder marker.
 *
 * Batch 1 clinical appendix: DRM does not publish a body-weight hydration
 * formula or a personalized fluid target. This constant is a neutral,
 * non-clinical reminder marker used only for the water ring's visual scale.
 * Members with a clinician-set fluid limit follow that advice instead.
 */
export const DEFAULT_WATER_TARGET_OZ = 64;
