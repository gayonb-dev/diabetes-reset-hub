# Batch 1 — Complete Parts A–I Report

Project: `wqennhjdojjqmmqzjhti` (production preflight confirmed)
Status: technical work complete. **Not published.** No live payments, no real email,
no external AI call, no mutation of real member data, no claim of clinical approval.

---

## Part A — Corrected content inventory (QA findings applied)

Regenerated by `scripts/doctor-review/build-inventory.py` after applying
`DRM_Batch1_Content_Inventory_QA_Findings.md`.

| Measure | Value |
| --- | --- |
| Inventory items | 1963 |
| Active items | 1935 |
| Member-reachable items | 1922 |
| `daily_actions` records reconciled | **187** |
| — active guided days | 180 |
| — historical/unreachable (E1–E7) | 7 |
| Flagged items (non-KEEP) | 211 |

Items by source type: database 870, source 921, admin-editable 159, seed/default 13.

Dispositions: KEEP — APPROVED EDUCATION 1752; REWRITE — CLINICIAN REVIEW 93;
REWRITE — OWNER APPROVAL 36; RETIRE — OBSOLETE FEATURE 23; RETIRE —
OUTCOME/GAMIFICATION 6; TEMPORARY FALLBACK APPLIED 12; HISTORICAL — UNREACHABLE 41;
FIX INTERACTION — NONFUNCTIONAL 0.

### Reachable-status proof
- Every member read path for guided days (`Dashboard.tsx`, `DayDetail.tsx`, MCP
  `get-today-action`, edge `mcp`) filters `is_extension_day = false`, so the seven
  E1–E7 rows were never selectable by a member.
- Those seven rows are now `is_active = false` and carry the
  `HISTORICAL — UNREACHABLE` disposition (gate `duplicate_day_reconciliation`
  fails the build if any inactive row lacks it).
- Idempotent constraint in place: unique index `daily_actions_one_active_per_day`
  on `(day_number) WHERE is_active` — verified present, so a program day can never
  acquire a second active guided record. Live check: 187 total = 180 active + 7 historical.

### Personal-data protection in the inventory
The doctor-review pack reviews **authored content only**. Fifteen member-owned tables
(`profiles`, `visitor_profiles`, `support_tickets`, `support_ticket_notes`,
`points_ledger`, `community_questions`, `community_answers`, `win_posts`,
`qa_submissions`, `messages`, `conversations`, `health_logs`,
`blood_sugar_readings`, `member_measurements`, `intake_submissions`) are declared
`EXCLUDED — member personal data` in the coverage manifest with a documented reason.

New fail-closed gate `no_personal_data` (in `build-inventory.py`) inspects the SQL the
generator actually issues and every emitted item's location; the run aborts if either
touches an excluded table. Gate result: **PASS**. Recorded in the JSON under
`gates.no_personal_data` and `personal_data_policy`.

Clinical copy remains **unapproved**. No clinical wording was invented; the only
replacement copy present is the previously approved temporary fallback.

---

## Part I gate results

| Gate | Result | Evidence |
| --- | --- | --- |
| Classifier fixtures | PASS | 38 fixtures, run before artifacts regenerate |
| Coverage manifest | PASS | every declared surface has items or a documented reason |
| Duplicate-day reconciliation | PASS | 187 = 180 active + 7 historical, all dispositioned |
| No personal data in inventory | PASS | new fail-closed gate, 15 excluded tables |
| Count reconciliation | PASS | json=1963, csv=1963, flagged=211, matrix=211, pack=211 |
| TypeScript (`tsgo --noEmit`) | PASS | no diagnostics |
| Test suite | PASS | 33 files / 364 tests |
| Timezone boundaries | PASS | `calendarDay.test.ts`, `calendarDayParity.test.ts` |
| RLS drift | PASS | 0 public tables with RLS disabled |
| Production build | PASS | built clean |
| Bundle purity | PASS | no source maps emitted; 0 hits for retired claims |

Largest bundles: `index` 426,854 B (134 kB gzip), `LineChart` 383,044 B (105 kB gzip),
`supabase` 171,496 B (45 kB gzip).

---

## Support diagnostics — controlled schema removal

Migration `20260816052235_support_diagnostics_daily_safeguard.sql` ran **fail-closed**:
it aborts unless `support_tickets.user_agent` exists AND holds zero non-null values.

- Observed non-null `user_agent` count at migration time: **0**.
- Column dropped. Live verification: `user_agent` column count = 0.
- Replaced with the minimum non-identifying diagnostics, both CHECK-constrained:
  - `client_platform` ∈ {web, ios, android, unknown}
  - `client_viewport` ∈ {mobile, desktop}
- `src/pages/app/Support.tsx` no longer reads `navigator.userAgent`; it sends
  `clientPlatform: "web"` and a viewport bucket derived from window width.
- `supabase/functions/support-request/index.ts` no longer accepts or stores a user
  agent; it validates and persists only the two constrained fields. Redeployed.
- `supabase/functions/_shared/inventory.ts`: the `redact: ["user_agent"]` entry for
  `support_tickets` is gone (nothing to redact) and replaced with a note that the raw
  user agent is not collected. Existing global redaction of `user_agent` /
  `user_agent_hash` for legacy tables is unchanged.

---

## Activity Score — canonical source and legacy reconciliation

`public.points_ledger` is the **only** Activity Score authority.

- Write path: `award_points` RPC (idempotency-keyed), called from `gamify-action`.
- Read path: `useActivityScore.ts` → the sole numeric surface, `Profile.tsx`, plus
  the member-visible `ActivityScoreCard` which lists the same ledger rows.
- `visitor_profiles.reset_points` is no longer rendered anywhere.

Live reconciliation:

| Source | Rows | Total |
| --- | --- | --- |
| `points_ledger` | 0 | 0 points, 0 users |
| `visitor_profiles.reset_points` (legacy Activity Score) | 0 users > 0 | 0 |
| `user_streaks.total_xp` (legacy streak XP — *not* Activity Score) | 1 user | 2000 |

Result: ledger (0) reconciles exactly with legacy Activity Score points (0), so **no
backfill is required and none was invented**. The 2000 legacy XP belongs to the streak
and earned-level system, which is derived from completed program days, not from the
points ledger; it is deliberately left untouched and is not shown as an Activity Score.

Where the ledger cannot account for a number, `Profile.tsx` now renders `—` plus
"Your Activity Score is being migrated…" instead of a legacy value that would
contradict the ledger entries shown directly beneath it.

---

## Files changed this pass

- `supabase/migrations/20260816052235_support_diagnostics_daily_safeguard.sql` (applied)
- `src/pages/app/Support.tsx`
- `supabase/functions/support-request/index.ts` (redeployed)
- `supabase/functions/_shared/inventory.ts`
- `src/pages/app/Profile.tsx`
- `scripts/doctor-review/build-inventory.py`
- `docs/doctor-review/*` (four artifacts regenerated) + this report

## Outstanding (not blocking technical closeout)

- 129 items await external clinical/owner review (93 clinician, 36 owner). No clinical
  approval is claimed.
- Publication is deliberately withheld.
